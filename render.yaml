services:
  - type: web
    name: backend-app
    runtime: docker
    rootDir: ./backend
    dockerContext: .
    plan: free
    healthCheckPath: /api/hello

  - type: web
    name: frontend-app
    runtime: docker
    rootDir: ./frontend
    dockerContext: .
    plan: free
    envVars:
      - key: VITE_API_URL
        value: https://replace-this-after-backend-deploy.onrender.com
